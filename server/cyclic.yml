version: "1"
build:
  steps:
    - name: "Install Dependencies"
      command: "npm run build"
  artifacts:
    - path: "../client/my-app/build/**/*"
deploy:
  steps:
    - name: "Deploy to Server"
      command: "npm run start"

